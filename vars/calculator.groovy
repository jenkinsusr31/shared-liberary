def add(a,b){
	echo "Build Number is ${BUILD_NUMBER}"
	echo "Node Name is ${NODE_NAME}"
	echo "Addition of ${a} and ${b} is ${a+b}"
}
def mul(a,b){
	echo "Multiplication of ${a} and ${b} is ${a*b}"
}
def div(a,b){
	echo "Division of ${a} and ${b} is ${a/b}"
}
def modu(a,b){
	echo "Remaindedr of ${a} and ${b} is ${a%b}"
}

